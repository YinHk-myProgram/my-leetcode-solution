/**
 * @param {number[][]} matrix
 * @param {number} target
 * @return {boolean}
 */
var searchMatrix = function(matrix, target) {
    let m = matrix.length
    let n = matrix[0].length
    for(let i=0;i<=m-1;i++) {
        for(let j=0;j<=n-1;j++) {
            if(matrix[i][j]==target) {
                return true
                break
            }
               
        }
    }
    return false
};
